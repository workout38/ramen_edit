<!DOCTYPE html>
<html>
  <head>
    <title>RamenMaps</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <header>
        <div class="header-logo">
          <%= link_to("ラーメン マップ", "/") %>
        </div>
        <ul class="header-menus">
          <li>
            <%= link_to("ログイン", "/login") %>
          </li>
          <li>
            <%= link_to("新規登録", "/login") %>
          </li>
        </ul>
      </header>

      <% if flash[:notice] %>
        <div class="flash">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <%= yield %>

      <footer>
        <p>copy right. 2024</p>
      </footer>
  </body>
</html>
